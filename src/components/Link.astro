---
const {title, href, bool} =  Astro.props;
---

<a href={href} class=`font-sans text-2xl decoration-accent-300 decoration-4 hover:underline ${bool ? "underline text-background-100": "text-background-500"}`>{title}</a>